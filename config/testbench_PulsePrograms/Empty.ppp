const DDSDetect = 0

exitcode endLabel = 0xfffe000000000001
parameter initTime
shutter initShutter

#update(initTime)
set_shutter(initShutter)
set_shutter(initShutter)
set_shutter(initShutter)
set_shutter(initShutter)
update(initTime)
update(initTime)
update(initTime)
exit( endLabel )